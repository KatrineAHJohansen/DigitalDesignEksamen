---
const {title, textcontent, numbers, ctatext} = Astro.props;
---

<section>
    <h1>{title}</h1>
    <p>{textcontent}</p>
</section>

<style>
    section {
        display: inline-flex;
        flex-direction: column;
        justify-content: flex-start;
        width: 30rem;
        height: 10rem;
        color: var(--secondary-typeface-color);
        background-color:rgba(28, 30, 35, 0.80) ;
        margin-top: 2rem;
        margin-left: 1rem;
    }

</style>